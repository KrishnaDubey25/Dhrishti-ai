import {useState} from 'react';import {Link,useNavigate} from 'react-router-dom';import {Brand} from '../components/Common';import {useAuth} from '../contexts/AuthContext';import {registerDoctor} from '../services/db';
export function DoctorRegister(){const nav=useNavigate();const {setUser}=useAuth();const[n,setN]=useState('');const[e,setE]=useState('');const[p,setP]=useState('');const[err,setErr]=useState('');function submit(){try{if(!n||!e||p.length<6)throw new Error('Enter name, email and a password with at least 6 characters.');const u=registerDoctor(n,e,p);setUser(u);nav('/doctor/dashboard')}catch(x:any){setErr(x.message)}}return <div className="auth-page"><div className="auth-art"><Brand/><div><h2>Clinical review stays separate from AI recommendation.</h2><p style={{opacity:.75}}>Development-only ophthalmologist workspace setup. Production deployment should connect verified clinician identity and authorization.</p></div></div><div className="auth-form"><div className="form-card"><h1>Ophthalmologist setup</h1>{err&&<div className="error">{err}</div>}<div className="field"><label>Full name</label><input className="input" value={n} onChange={x=>setN(x.target.value)}/></div><div className="field"><label>Email</label><input className="input" type="email" value={e} onChange={x=>setE(x.target.value)}/></div><div className="field"><label>Password</label><input className="input" type="password" value={p} onChange={x=>setP(x.target.value)}/></div><button className="btn primary" style={{width:'100%'}} onClick={submit}>Create doctor workspace</button><p className="small"><Link to="/login">Back to login</Link></p></div></div></div>}
